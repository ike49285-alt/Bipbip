pandas>=2.2
numpy>=1.26
pyarrow>=15.0
pyyaml>=6.0
yfinance>=0.2.40
scikit-learn>=1.4
# Used by bipbip/options/pricing.py (_norm_cdf) and the premium sign test.
# It has no fallback: without it every option price raises ImportError.
scipy>=1.11
